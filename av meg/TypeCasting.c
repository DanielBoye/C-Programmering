#include <stdio.h>

int main(void)
{
    int slices = 17;
    int people = 2;
    
    double halfThePizza = (double) (slices) / people; 
    
    printf("%f", halfThePizza);
    
    
    
    return 0;
}